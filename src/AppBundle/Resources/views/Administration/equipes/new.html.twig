{% extends '::base-admin.html.twig' %}

{% block breadcrumbs %}
	<a href="{{ path('administration_equipes_index') }}">Equipes</a> / Création /
{% endblock %}

{% block content %}
	<h1>Création d'une équipe</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<input type="submit" value="Create"/>
	{{ form_end(form) }}

	<ul>
		<li>
			<a href="{{ path('administration_equipes_index') }}">Back to the list</a>
		</li>
	</ul>
{% endblock %}
