{% extends '::base-admin.html.twig' %}

{% block breadcrumbs %}
	<a href="{{ path('administration_equipes_index') }}">Equipes</a> / Création /
{% endblock %}

{% block content %}
	<h1>Création d'une équipe</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
    {{ include('@App/Administration/footer-new.html.twig', {var: 'equipes'}) }}
	{{ form_end(form) }}
{% endblock %}
