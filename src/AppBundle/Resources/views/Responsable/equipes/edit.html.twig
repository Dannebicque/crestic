{% extends '::base-responsable.html.twig' %}

{% block breadcrumbs %}
	<a href="{{ path('responsable_equipes_index') }}">Vos Equipes</a> / Modification /
{% endblock %}

{% block content %}
    <h1>Modification d'une équipe</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        {{ include('@App/Responsable/footer-edit.html.twig', {var: 'equipes'}) }}
    {{ form_end(edit_form) }}
{% endblock %}
